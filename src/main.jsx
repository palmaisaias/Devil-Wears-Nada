// src/main.jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css' // ← the only global CSS import

ReactDOM.createRoot(document.getElementById('root')).render(<App />)
